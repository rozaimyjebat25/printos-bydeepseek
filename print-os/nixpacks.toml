# Nixpacks configuration for PRINT OS API
# Railway akan auto-detect ini dan run build dari api/

[phases.setup]
nixPkgs = ["nodejs-20_x", "npm-10_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
